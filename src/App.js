import React from 'react';

const App = () => {
    return (
        <div style={ { display: 'flex', justifyContent: 'center' } }>
            <div style={ { width: '70%' } }>
                <div>Hello world!</div>
            </div>
        </div>
    );
};

export default App;
